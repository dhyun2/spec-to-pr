# spec-to-pr 프로젝트 개선 과제

> 2026-07-08 코드 전수 조사 결과. 각 항목은 근거 파일:라인과 함께 기재했으며, 이 문서만 읽고 작업을 시작할 수 있도록 작성됨.
> 우선순위: P0(버그·즉시) > P1(호환성·기능 공백) > P2(개선·리팩터).

## 구현 결과 요약 (2026-07-08 반영)

| 항목                           | 상태    | 요지                                                                     |
| ------------------------------ | ------- | ------------------------------------------------------------------------ |
| P0-1 GitHub 이미지 엑박        | ✅ 완료 | commit SHA 고정 raw URL + private repo 감지 시 링크 폴백(`embeddable`)   |
| P0-2 GitLab 이미지 엑박        | ✅ 완료 | 인스턴스 루트 절대화 제거, project-relative 업로드 경로 유지             |
| P1-1 Codex 서브에이전트 비대칭 | ✅ 완료 | `.codex/agents/` 13개 추가(총 16), lane/리뷰 스킬 9개에 순차 폴백 명문화 |
| P1-2 Council 재검토 루프       | ✅ 완료 | 오케스트레이터에 category별 lane 재실행 + 최대 2회 bounded 루프          |
| P1-3 self-hosted GitLab/GHE    | ✅ 완료 | 호스트 휴리스틱 + `SPEC_TO_PR_GIT_HOST/API/WEB` override                 |
| P1-4 토큰 CLI 폴백             | ✅ 완료 | env 우선, 없으면 `gh auth token`/`glab auth token`                       |
| P2-1 GitLab 테스트 픽스처      | ✅ 완료 | 실제 GitLab 응답(`url`/`full_path`/`markdown`) 형태로 교정               |
| P2-2 figma-doctor agent 부재   | ✅ 완료 | docs 웹에서 스킬 단독으로 표기                                           |
| P2-3 Codex frontmatter 문서화  | ✅ 완료 | `docs/codex/README.md`에 무시 키 표 + self-hosted/토큰 안내              |
| P2-4 publish 부분 실패 처리    | ✅ 완료 | asset 업로드 실패 시 preview 없이 발행(전체 실패 방지)                   |

검증: `pnpm typecheck · build · test(288 pass) · format:check · plugin:validate(claude+codex)` 전부 통과.

> 아래는 원래 조사 내용(설계 근거 보존). 각 항목의 "해결 방향"은 위 표대로 구현됨.

---

## P0-1. GitHub PR 이미지 엑박 (깨진 이미지)

### 증상

PR 본문의 "Visual Evidence Preview" 섹션 이미지가 가끔 엑박으로 뜸.

### 원인 (코드로 확정)

`src/publisher/github-publisher.ts:119-179 publishAssets()`가:

1. **PR 소스 브랜치에 Contents API로 이미지를 커밋**함
   (`.spec-to-pr/visual-assets/{runId}/{targetId}/{filename}`, branch: `input.payload.sourceBranch` — 143-155행)
2. 응답의 `download_url`을 PR 본문에 임베드함 (165행)
   → `https://raw.githubusercontent.com/{owner}/{repo}/{branch}/{path}` 형태

이 방식의 3가지 문제:

| #   | 문제                                                                                                        | 엑박이 뜨는 조건       |
| --- | ----------------------------------------------------------------------------------------------------------- | ---------------------- |
| 1   | **private repo**에서 raw.githubusercontent.com URL은 인증 필요. GitHub camo 프록시가 비인증으로 fetch → 404 | private repo면 항상    |
| 2   | URL이 **브랜치명 기준**. merge 후 브랜치 삭제 시 404                                                        | merge + 브랜치 삭제 후 |
| 3   | (부작용) 이미지가 **PR diff에 포함**되어 main으로 merge됨 — 바이너리로 저장소 오염                          | 항상                   |

### 해결 방향 (권장 순서)

1. **전용 orphan assets 브랜치** 사용 (예: `spec-to-pr/assets`, PR 대상이 아님)
   - PR diff 오염 제거 (문제 3 해결)
2. 임베드 URL을 **commit SHA 고정**으로 변경:
   `https://raw.githubusercontent.com/{owner}/{repo}/{commitSha}/{path}`
   - Contents API PUT 응답의 `commit.sha`를 사용하면 됨
   - 브랜치 삭제와 무관해짐 (문제 2 해결, public repo 완결)
3. **private repo 감지 시 폴백**: GitHub는 API로 user-attachments 업로드를 지원하지 않으므로,
   private repo에서는 `<img>` 임베드 대신 **파일 링크(blob URL) + artifact ID 표기**로 폴백하고
   PR 본문에 "private 저장소는 이미지 인라인 미리보기가 제한됩니다" 안내 문구 추가.
   - repo private 여부는 `GET /repos/{owner}/{repo}` 의 `private` 필드로 확인 가능
   - (선택) release asset 업로드 방식 검토 — 단 private release asset도 비인증 렌더 불가라 근본 해결은 아님
4. 테스트 갱신: `tests/unit/github-publisher.test.ts:60` 의 `download_url` 픽스처를 SHA-고정 URL 검증으로 교체.

---

## P0-2. GitLab MR 이미지 엑박

### 원인 (코드로 확정)

`src/publisher/gitlab-publisher.ts:118-164 publishAssets()`는 올바른 API(`POST /projects/:id/uploads`)를 쓰지만, URL 조립이 깨질 수 있음:

- 150행: `full_path ?? url` 을 취한 뒤
- 158행 + 213-219행 `absoluteGitLabAssetUrl()`: 상대경로면 `target.webBaseUrl` 기준으로 절대화
- 그런데 `src/publisher/remote-detector.ts:41` 에서 **`webBaseUrl = "https://gitlab.com"` (인스턴스 루트)**

GitLab uploads API의 `url` 필드는 **프로젝트-상대** 경로(`/uploads/{hash}/{file}`)임.
→ `full_path`가 없는(구버전) GitLab 응답에서는 `https://gitlab.com/uploads/{hash}/{file}` 이 됨
→ **프로젝트 경로(`/{group}/{project}`)가 빠진 URL → 404 엑박**.

추가 문제: `tests/unit/gitlab-publisher.test.ts:49` 픽스처가 `full_path: "/uploads/abc123/figma.png"` 로 되어 있는데, 실제 GitLab의 `full_path`는 `/{namespace}/{project}/uploads/...` 형태 — 테스트가 실제 API와 다른 모양을 검증 중.

### 해결 방향

1. **MR description에는 절대 URL 대신 GitLab 방식의 상대 참조를 사용**:
   uploads API 응답의 **`markdown` 필드**(`![file](/uploads/{hash}/{file})`)를 그대로 쓰거나 상대 `url`을 쓰면
   GitLab이 프로젝트 스코프로 해석 → private 프로젝트 포함 항상 렌더됨. (canonical한 방법)
   - 구현 영향: `PublishedReviewAsset.url`이 절대 URL이어야 한다는 zod 제약(`publish-contracts.ts`)과
     `publisher-service.ts:919`의 `<img src>` 렌더를 GitLab에서는 상대경로 허용으로 완화 필요
2. 절대 URL이 꼭 필요하면 `webBaseUrl + "/" + projectPath` 를 base로 절대화 (인스턴스 루트 금지)
3. 테스트 픽스처를 실제 GitLab 응답 형태(`url: "/uploads/..."`, `full_path: "/{ns}/{proj}/uploads/..."`, `markdown: "![...](...)"`)로 교정

---

## P1-1. Claude ↔ Codex 서브에이전트 비대칭 (Codex에서 lane이 안 돎)

### 현황 (파일로 확정)

- Claude용 agent: `agents/*.md` **15개**
  (accessibility-reviewer, api-contract, design-ui, eval-reviewer, integrator, observability-reviewer, openspec-archive-reviewer, performance-reviewer, pr-report-reviewer, publisher-reviewer, release-reviewer, review-council, security-hardening-reviewer, spec-bdd, visual-regression-reviewer)
- Codex용 agent: `.codex/agents/*.toml` **3개뿐**
  (spec-to-pr-review-council, spec-to-pr-visual-regression-reviewer, spec-to-pr-design-ui-repair)

스킬들이 이름으로 호출하는 subagent(스킬 본문 grep 결과 7종):
`accessibility-reviewer` `api-contract` `design-ui` `integrator` `pr-report-reviewer` `review-council` `visual-regression-reviewer`

→ **Codex에서는 `api-contract`, `design-ui`, `integrator`, `accessibility-reviewer`, `pr-report-reviewer` (및 spec-bdd lane)가 named subagent로 존재하지 않음.**
`docs/codex/README.md`는 "host가 subagent를 지원하지 않으면 같은 lane을 순차 실행" 이라고 하지만, **각 스킬 SKILL.md에는 그 폴백 절차가 없음** (예: `skills/run-design-ui/SKILL.md:54` "Invoke the `design-ui` subagent." 뿐).

### 해결 방향

1. `.codex/agents/` 에 누락 TOML 추가: 최소 `spec-to-pr-spec-bdd`, `spec-to-pr-api-contract`, `spec-to-pr-design-ui`, `spec-to-pr-integrator` (+ 게이트 리뷰어들). 기존 3개 TOML 형식(name/description/developer_instructions/model_reasoning_effort)을 따르면 됨.
2. lane/리뷰 스킬 SKILL.md에 공통 폴백 문단 추가:
   - "named subagent가 없거나 host가 subagent를 지원하지 않으면, 동일한 지시를 현재 스레드에서 순차 수행하고 동일한 record_* tool로 결과를 기록한다."
   - 대상: run-spec-bdd, run-api-contract, run-design-ui, run-integration, run-review-council, run-visual-regression, run-accessibility-gate, generate-pr-report, publish-review-request
3. Codex agent 이름 매핑 명시: 스킬 본문에서 `design-ui` ↔ Codex `spec-to-pr-design-ui` 처럼 host별 이름을 병기.

### 참고 (이미 잘 된 것)

- MCP 네임스페이스 이중 표기는 처리됨: 모든 SKILL.md frontmatter가 `mcp__spec-to-pr__*` + `mcp__spec_to_pr__*` 둘 다 나열, 본문에 host별 안내 있음.
- `allowed-tools` frontmatter는 Claude 전용이지만 Codex가 무시하므로 치명적이지 않음(문서화만 하면 됨).

## P1-2. Review Council 재검토(랄프) 루프 미지정

### 현황

- **시각 수리 루프는 정상 구현·명세됨** ✓
  - `src/visual/visual-repair-policy.ts:9-10` — `minPassingScore` 기본 **0.9**, `maxAttempts` 기본 **3** (최대 20)
  - `evaluate_visual_repair_loop` 가 `passed / retry / failed` 판정
  - `skills/run-visual-repair-loop/SKILL.md` — attempt마다 Design/UI 재실행→재캡처→재비교, gap ID·visual report를 다음 attempt에 주입, max 초과 금지
  - 오케스트레이터 `skills/spec-to-pr/SKILL.md:49` — "passed 또는 human-review blocker까지 반복" 명시
- **Council 루프는 없음** ✗
  - `skills/spec-to-pr/SKILL.md` 11단계는 "Run Review Council before final reporting" 뿐.
  - verdict가 `changes_requested`일 때 **어느 lane을 어떻게 재실행하고, 몇 번까지 재시도하고, 언제 blocked로 확정하는지**가 어디에도 없음.
  - Integration의 bounded repair(3회)는 별개로 존재함 — merge conflict 전용.

### 해결 방향

1. `skills/spec-to-pr/SKILL.md` 11단계를 bounded loop로 확장:
   - `changes_requested` → finding의 category(spec/api/design)별로 해당 lane만 재실행 → Council 재소집
   - 최대 attempt(권장 2회, visual repair와 별도 카운트) 초과 시 open gap과 함께 blocked 리포트로 종료
2. (선택) 서버 측 지원: `record_review_council_result`에 `attempt` 필드를 받아 Run ledger에 회차를 남기면 재현성이 좋아짐. visual-repair-policy.ts를 참고해 `council-repair-policy` 모듈을 추가하는 방식.
3. quality gate 실패 시 정책도 명시: 현재는 blocked 유지만 규정 — "실패한 check에 대응하는 lane 1회 재실행 허용" 같은 규칙을 넣을지 결정 필요.

## P1-3. self-hosted GitLab / GitHub Enterprise 미지원

`src/publisher/remote-detector.ts:47` — `github.com` / `gitlab.com` 외 remote는 `Unsupported Git remote host` throw.
사내 GitLab(예: gitlab.company.com)에서 publish가 아예 불가.

### 해결 방향

- remote host가 미지원이어도 **환경변수 override** 허용:
  `SPEC_TO_PR_GIT_HOST=gitlab`, `SPEC_TO_PR_API_BASE_URL`, `SPEC_TO_PR_WEB_BASE_URL`
- 또는 heuristics: remote URL에 `gitlab`이 포함되면 gitlab로 추정하고 apiBaseUrl을 `https://{host}/api/v4`로 구성.

## P1-4. 토큰 공급이 환경변수뿐

`src/publisher/token-provider.ts:10-13` — `GITHUB_TOKEN`/`GH_TOKEN`, `GITLAB_TOKEN`/`GITLAB_PRIVATE_TOKEN`만 읽음.

### 해결 방향

- `gh auth token` / `glab auth token` CLI 폴백 추가 (설치되어 있으면 exec해서 취득, 없으면 기존 env 에러 메시지).
- 에러 메시지에 두 경로(env, CLI) 모두 안내.

---

## P2-1. GitLab full_path 테스트 픽스처 교정

P0-2에 포함되나 독립 작업 가능. `tests/unit/gitlab-publisher.test.ts:49`의 `full_path`를 실제 GitLab 형태(`/{ns}/{proj}/uploads/...`)로 바꾸고, `url`/`markdown` 필드도 함께 검증.

## P2-2. figma-doctor "agent" 부재 정리

`skills/figma-doctor` 스킬은 있으나 `agents/figma-doctor.md`는 없음. 스킬 단독 동작이 의도라면 문서·주석에 명시, reviewer가 필요하면 agent 추가. (docs 웹은 이미 수정됨 — 스킬 단독으로 표기.)

## P2-3. Codex에서 skills frontmatter 호환 문서화

`allowed-tools`, `disable-model-invocation`, `argument-hint`는 Claude Code 규약. Codex가 어떤 키를 무시하는지 `docs/codex/README.md`에 표로 명시해 두면 디버깅 시간 절약.

## P2-4. publish 재시도/부분 실패 처리

`publishAssets`는 asset 하나 실패 시 전체 throw. 이미지 1장 실패로 PR 발행 전체가 죽지 않도록: 실패 asset은 gap(publish)으로 기록하고 표에는 artifact ID만 남기는 부분 성공 모드 검토.

---

## 검증 체크리스트 (작업 후)

```bash
pnpm format:check && pnpm typecheck && pnpm schemas:build && pnpm build && pnpm test
pnpm plugin:validate        # claude + codex 둘 다
```

- GitHub public/private repo 각각에서 publish 후 PR 본문 이미지 렌더 확인 (merge+브랜치삭제 후에도)
- GitLab(사내 인스턴스 포함)에서 MR description 이미지 렌더 확인
- Codex 스레드에서 `/spec-to-pr` end-to-end: lane 3개가 (subagent 또는 순차 폴백으로) 모두 record_* 까지 도달하는지
- Council `changes_requested` 시나리오에서 루프가 상한 내에 종료되는지
